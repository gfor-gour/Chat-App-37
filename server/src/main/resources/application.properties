# Server Configuration
server.port=9999
server.host=0.0.0.0

# Database Configuration (PostgreSQL)
db.driver=org.postgresql.Driver
db.url=jdbc:postgresql://localhost:5432/chat_application

db.username=postgres
db.password=gourPostGress.37
db.pool.size=10

# File Transfer Configuration
file.storage.path=server_data/
file.max.size.mb=50
file.allowed.extensions=.jpg,.jpeg,.png,.gif,.pdf,.doc,.docx,.zip,.txt,.mp3,.mp4

# WebSocket Configuration
socketio.ping.interval=25000
socketio.ping.timeout=60000
